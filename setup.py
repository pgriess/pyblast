from distutils.core import setup

setup(
    name='PyBlast',
    version='0.1',
    description='Easy-to-use wrapper for NCBI BLAST',
    author='Peter Griess',
    author_email='pg@std.in',
    py_modules=['pyblast'])
